apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: static-data-pvc-awx
  namespace: awx
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 20Gi
